import * as React from 'react';

import ReactDOM from 'react-dom';

import { PopupBox } from './react-revolution/public/react-revolution';

import './react-revolution/sass/rr-popupbox.scss';

/**
 * Render it to the html while testing
 */
class App extends React.Component {
    constructor(props) {
        super(props);


        this.state = {
            moreData: [

            ]
        }
    }

    render() {

        return (
            <span>
                <PopupBox
                    defaultClass={false}
                    id={false}
                    animationTime={300}
                    animationType='top-left' // top-left, top-right, bottom-left, bottom-right
                    icon={
                        <i className='fas fa-user-secret icon-chat popup-box-icon'></i>
                    }
                    data={
                        <span>
                            <h1>
                                <i className='fas fa-user-secret icon-chat' />
                                Box title
                            </h1>

                            
                        </span>
                    }
                />
            </span>
        )
    }
}

ReactDOM.render(<App />, document.getElementById('app'));