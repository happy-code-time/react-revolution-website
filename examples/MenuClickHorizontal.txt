import * as React from 'react';

import ReactDOM from 'react-dom';

import MenuClickHorizontal from './module/source/MenuClickHorizontal';

/**
 * Render it to the html while testing
 */

import './module/source/sass/rr-menu-click-horizontal.scss';

class App extends React.Component
{
    constructor(props){
        super(props);

        this.state = {
            data: [
                {
                    text: 'click me 1',
                    dataToggle: 'child 1',
                    dataToggleAttributes: {
                        
                    },
                    classList: 'test1'
                },
                {
                    text: 'click me 1',
                    dataToggle: 'child 1',
                    classList: 'test2',
                    data: [
                        {
                            text: 'click me 2',
                            dataToggle: 'child 2',
                            dataToggleAttributes: {},
                            classList: 'test3'
                        },
                    ]
                }
            ]
        }
    }

    render(){
        const { data } = this.state;

        return(
            <span>
                <MenuClickHorizontal
                    // defaultClass='default-class'
                    data={data}
                />
            </span>
        )
    }
}

ReactDOM.render(<App />, document.getElementById('app'));